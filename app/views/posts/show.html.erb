<% if flash[:notice] %>
    <%= flash[:notice]  %>
<% elsif flash[:alert] %>
    <%= falsh[:alert] %>
    <% end  %>


<h1><%= @post.title %></h1>
<%= link_to "edit" , edit_post_path(@post)%>
|
<%= link_to "Delete", post_path(@post),
                      method: :delete,
                      data: {confirm: "Are you sure?"}  %>
