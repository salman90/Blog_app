<p>this will be the form</p>

<%= form_for @post do |f| %>
<div>
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>
<div>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <div>
  <%= f.collection_select :category_id, Category.all, :id, :title %>
</div>
</div>
<%= f.submit %>
<% end %>
